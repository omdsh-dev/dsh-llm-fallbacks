---
category: Changed
---
- The host model picker now labels the virtual row `Auto: <slot>[<model>]` (e.g. `Auto: Liang Peak[deepseek-v4-flash]`) instead of a static `Auto`; the catalog id stays `Auto`.
