---
category: Added
---
- FallbacksChain appears in the model picker when fallbacks is enabled and a conforming all-day root chain is configured; selecting it uses the chain as the root primary, while selecting a real model keeps fallback-only behavior.
